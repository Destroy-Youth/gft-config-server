server:
  port: 8102

#############    Local resources   ################
spring:
  application:
    name: config-server
  profiles:
     active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:config/

#############    Git repo resources   ################
#spring:
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/Destroy-Youth/config-repo.git
#          clone-on-start: true
